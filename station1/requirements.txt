# Station 1 — Pinned dependencies
# PyTorch CPU/CUDA: install via pip. Docker uses CPU-only index (see Dockerfile).
torch==2.6.0
transformers==4.46.3
fastapi==0.115.5
uvicorn[standard]==0.32.1
numpy>=2.0
pydantic==2.9.2
httpx==0.27.2
pytest==8.3.4
